name: Build WatermarkApp

on:
  push:
    branches: [ main, master ]
    paths:
      - 'WatermarkApp/**'
      - '.github/workflows/build.yml'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v1

    - name: Setup NuGet
      uses: NuGet/setup-nuget@v1

    - name: Restore NuGet packages
      run: nuget restore WatermarkApp\WatermarkApp.csproj

    - name: Build Release
      run: msbuild WatermarkApp\WatermarkApp.csproj /p:Configuration=Release /p:Platform=AnyCPU

    - name: Prepare installer staging
      shell: pwsh
      run: |
        New-Item -ItemType Directory -Force output | Out-Null
        Copy-Item WatermarkApp\bin\Release\WatermarkApp.exe output\
        Copy-Item WatermarkApp\config.ini output\
        Write-Host "Staging files:"
        Get-ChildItem output\

    - name: Install Inno Setup
      run: choco install innosetup -y --no-progress

    - name: Build Installer
      shell: pwsh
      run: |
        $iss = Get-Content WatermarkApp\setup.iss -Raw
        # 重定向源路径到 staging 目录
        $iss = $iss -replace 'Source: "bin\\Release\\', 'Source: "output\'
        $iss = $iss -replace 'Source: "config\.ini"', 'Source: "output\config.ini"'
        $iss | Set-Content setup_ci.iss

        Write-Host "=== Modified setup.iss ==="
        Get-Content setup_ci.iss | Select-String "Source:"

        & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" setup_ci.iss

    - name: Upload Installer
      uses: actions/upload-artifact@v4
      with:
        name: WatermarkApp-Setup
        path: Output\WatermarkApp_Setup.exe
        if-no-files-found: error

    - name: Upload Portable Build
      uses: actions/upload-artifact@v4
      with:
        name: WatermarkApp-Portable
        path: |
          WatermarkApp\bin\Release\WatermarkApp.exe
          WatermarkApp\config.ini
